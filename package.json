{
    "name": "picow-rgbw-app",
    "private": true,
    "version": "2.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite --host",
        "build": "vite build",
        "build:windows": "npm run build && rm -r build/wails/frontend/src/* && cp -r dist/* build/wails/frontend/src/ && cd build/wails && wails build && wails build -nsis",
        "build:linux": "npm run build && rm -r build/wails/frontend/src/* && cp -r dist/* build/wails/frontend/src/ && cd build/wails && wails build",
        "build:android": "npm run build && npx cap sync android && npx cap open android",
        "preview": "vite preview"
    },
    "devDependencies": {
        "@sveltejs/vite-plugin-svelte": "^2.4.2",
        "svelte": "^4.0.5",
        "svelte-css": "../svelte-css",
        "svelte-material-icons": "^3.0.5",
        "vite": "^4.4.5"
    },
    "dependencies": {
        "@capacitor/android": "^5.2.2",
        "@capacitor/cli": "^5.2.2",
        "@capacitor/core": "^5.2.2"
    }
}
